const assert=require('node:assert/strict'),fs=require('fs'),vm=require('vm');let installed,started,alarmListener,created,badge;const chrome={runtime:{onInstalled:{addListener:f=>installed=f},onStartup:{addListener:f=>started=f}},alarms:{clear:async()=>true,create:(name,opts)=>created={name,...opts},onAlarm:{addListener:f=>alarmListener=f}},action:{setBadgeBackgroundColor:async()=>{},setBadgeText:async x=>badge=x.text}};vm.runInNewContext(fs.readFileSync('./background.js','utf8'),{chrome,Date,setTimeout});(async()=>{await installed();assert.equal(created.name,'reachr-daily-review');assert.equal(created.periodInMinutes,1440);assert.ok(created.when>Date.now());await alarmListener({name:'reachr-daily-review'});assert.equal(badge,'50');console.log('daily alarm tests passed')})().catch(e=>{console.error(e);process.exit(1)});
